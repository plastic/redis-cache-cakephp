# Redis Cache Engine for CakePHP

## Installation

1. Place the ```redis.php``` into ```cake/libs/cache/```.
2. Place the ```redisent.php``` into ```app/libs/```.
3. Open ```[yourapp]/config/core.php``` and write ```Cache::config('redis');```.